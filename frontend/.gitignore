# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# ad hoc smoke-test scripts/output, never committed
.smoke*.mjs
.smoke-screenshots/
.e2e*.mjs
.e2e-screenshots/

# Intermediate build artifact for `npm run generate-types` (scripts/export_openapi.py ->
# openapi.json -> src/types/api-generated.ts, which IS committed). Regenerate on demand rather
# than committing a copy that can silently drift from the generated types it produced.
openapi.json

# Storybook
storybook-static/

# Vitest coverage
coverage/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
